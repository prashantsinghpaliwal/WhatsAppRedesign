<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:padding="12dp">

    <com.bigstep.whatsappredesign.ui.views.CircularImageView
        android:id="@+id/contact_image"
        android:layout_width="56dp"
        android:layout_height="56dp"
        app:civ_border="true"
        app:civ_border_color="@android:color/white"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@tools:sample/avatars" />

    <TextView
        android:id="@+id/contact_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="4dp"
        android:text="Jammie Paterson"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toRightOf="@id/contact_image"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/message"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="12dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="Hey There! I am using WhatsApp Hey There! I am using WhatsApp Hey There! I am using WhatsApp"
        app:layout_constraintLeft_toRightOf="@id/contact_image"
        app:layout_constraintRight_toLeftOf="@id/message_status_img"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/contact_name" />

    <ImageView
        android:id="@+id/message_status_img"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:padding="@dimen/element_spacing_xsmall"
        android:visibility="visible"
        app:layout_constraintLeft_toRightOf="@id/message"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/contact_name"
        app:srcCompat="@drawable/ic_double_check" />

    <TextView
        android:id="@+id/unread_count"
        android:layout_width="@dimen/unread_indicator_size"
        android:layout_height="@dimen/unread_indicator_size"
        android:layout_marginStart="4dp"
        android:padding="2dp"
        android:background="@drawable/count_bg"
        android:text="3"
        android:textAlignment="center"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:visibility="gone"
        app:layout_constraintLeft_toRightOf="@id/message"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/contact_name" />

</androidx.constraintlayout.widget.ConstraintLayout>